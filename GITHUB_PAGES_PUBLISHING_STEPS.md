# Publish SOVIRA GLOBAL on GitHub Pages for Free

GitHub account detected from your message:

```text
https://github.com/LOPS6876
```

## Recommended free live URL

Create a GitHub Pages user-site repository named:

```text
lops6876.github.io
```

Your free website URL will be:

```text
https://lops6876.github.io/
```

Use this exact username-style repository, not a project repository such as `sovira-global`, because the website is built with clean root paths like `/assets/css/styles.css`, `/products/`, and `/contact/`.

## Browser upload steps

1. Sign in to GitHub as `LOPS6876`.
2. Click the **+** icon in the top-right corner.
3. Click **New repository**.
4. Repository name: `lops6876.github.io`.
5. Visibility: **Public**.
6. Do not add a license or `.gitignore` unless you want to.
7. Click **Create repository**.
8. Unzip this package on your computer.
9. Open the unzipped folder.
10. Select every file and folder inside it, including `index.html`, `assets`, `products`, `sitemap.xml`, `robots.txt`, `.nojekyll`, and all page folders.
11. Drag the selected files into the GitHub repository upload page.
12. Commit the files to the `main` branch.
13. Open **Settings > Pages**.
14. Under **Build and deployment**, set **Source** to **Deploy from a branch**.
15. Set **Branch** to `main` and folder to `/(root)`.
16. Click **Save**.
17. Open `https://lops6876.github.io/` after GitHub finishes deployment.

## Important notes

- Upload the contents inside the folder, not the zip file itself.
- Keep the repository public for free GitHub Pages publishing.
- The site is prepared for the future custom domain `https://soviraglobal.com` in canonical URLs and sitemap entries. For final SEO launch, connect the real domain before submitting the sitemap to Google Search Console.
- Forms need to be connected later to Formspree, Netlify Forms, EmailJS, a CRM, or a backend email service.
- Replace placeholder SVG product illustrations with real product photography when available.
- Add only verified certificates and documents.
- Review Privacy Policy and Terms & Conditions with legal counsel before public commercial launch.
